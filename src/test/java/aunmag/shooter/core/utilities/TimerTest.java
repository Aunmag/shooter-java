package aunmag.shooter.core.utilities;

/**
 * TODO: This is new. Complete it
 */
class TimerTest {
/*
    @Test
    void testInitializeAtTimeStart() {
        TimeFlow time = new TimeFlow();
        Timer timer = new Timer(time, 1);
        assertEquals(time, timer.time);
        assertEquals(1, timer.getDuration());
        assertEquals(0, timer.getInitial());
        assertEquals(0, timer.getPassed());
        assertEquals(1, timer.getRemain());
        assertEquals(1, timer.getTarget());
        assertFalse(timer.isDone());

        time = new TimeFlow();
        timer = new Timer(time, -1);
        assertEquals(-1, timer.getDuration());
        assertEquals(0, timer.getInitial());
        assertEquals(0, timer.getPassed());
        assertEquals(-1, timer.getRemain());
        assertEquals(-1, timer.getTarget());
        assertTrue(timer.isDone());
    }

    @Test
    void testInitializeLater() {
        TimeFlow time = new TimeFlow();
        time.add(10, true);
        Timer timer = new Timer(time, 5);
        assertEquals(5, timer.getDuration());
        assertEquals(10, timer.getInitial());
        assertEquals(0, timer.getPassed());
        assertEquals(5, timer.getRemain());
        assertEquals(15, timer.getTarget());
        assertFalse(timer.isDone());

        time = new TimeFlow();
        time.add(10, true);
        timer = new Timer(time, -5);
        assertEquals(-5, timer.getDuration());
        assertEquals(10, timer.getInitial());
        assertEquals(0, timer.getPassed());
        assertEquals(-5, timer.getRemain());
        assertEquals(5, timer.getTarget());
        assertTrue(timer.isDone());
    }

    @Test
    void testInitializeBefore() {
        TimeFlow time = new TimeFlow();
        time.add(-10, true);
        Timer timer = new Timer(time, 5);
        assertEquals(5, timer.getDuration());
        assertEquals(-10, timer.getInitial());
        assertEquals(0, timer.getPassed());
        assertEquals(5, timer.getRemain());
        assertEquals(-5, timer.getTarget());
        assertFalse(timer.isDone());

        time = new TimeFlow();
        time.add(-10, true);
        timer = new Timer(time, -5);
        assertEquals(-5, timer.getDuration());
        assertEquals(-10, timer.getInitial());
        assertEquals(0, timer.getPassed());
        assertEquals(-5, timer.getRemain());
        assertEquals(-15, timer.getTarget());
        assertTrue(timer.isDone());
    }

    @Test
    void testNext() {
        TimeFlow time = new TimeFlow();
        Timer timer = new Timer(time, 1);

        timer.setDuration(5);

        assertEquals(5, timer.getDuration());
        assertEquals(0, timer.getInitial());
        assertEquals(0, timer.getPassed());
        assertEquals(1, timer.getRemain());
        assertEquals(1, timer.getTarget());
        assertFalse(timer.isDone());
    }
*/
}
